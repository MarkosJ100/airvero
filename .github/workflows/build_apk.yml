name: Build Android APK

on:
  push:
    branches:
      - refactor/v2-admin-only
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build Web App
        run: npm run build
        env:
          VITE_SUPABASE_URL: https://valbywilixexmhbnssye.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_Lfq3VVRrJMeK6J6inezI_w_NPWE5by5
          VITE_GOOGLE_CLIENT_ID: 478300349997-90a57r4pui9q95c2b5f270dcrqtqnpn5.apps.googleusercontent.com

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Grant execute permission for gradlew
        run: chmod +x android/gradlew

      - name: Build Debug APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
